$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
code: model
command: >-
  python train.py
  --training_data ${{inputs.training_data}}
  --reg_rate ${{inputs.reg_rate}}
inputs:
  training_data:
    type: uri_folder
    path: ${AML_DATA_PATH_DEV}
  reg_rate: 0.01
environment: azureml://registries/azureml/environments/sklearn-1.5/versions/35
compute: azureml:mlops-classification-cluster
experiment_name: training-job-with-github-actions-dev
description: simple classification training job with github actions for dev
display_name: classification-diabetes-dev
tags:
    training: classification
    framework: sklearn
    environment: dev